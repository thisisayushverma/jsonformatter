export default function Head() {
    return (
      <>
        <title>XML to JSON Converter | Convert XML to JSON Online</title>
        <meta
          name="description"
          content="Convert XML to JSON easily. Supports attributes, nested nodes, and formatted JSON output."
        />
        <meta name="keywords" content="xml to json, convert xml json, xml parser" />
      </>
    );
  }
  