export default function Head() {
    return (
      <>
        <title>JSON Schema Validator | Validate JSON Schema Online</title>
        <meta
          name="description"
          content="Validate JSON against JSON Schema definitions. Detect missing fields, type mismatches, and schema violations instantly."
        />
        <meta name="keywords" content="json schema validator, json schema checker, validate json schema" />
      </>
    );
  }
  